#! /usr/bin/env node
// Store the famous person's name in a variable
let famous_person = "Albert Einstein";
// Define the quote
let quote = "Imagination is more important than knowledge. For knowledge is limited, whereas imagination embraces the entire world, stimulating progress, giving birth to evolution.";
// Compose the message
let message = `"${quote}" - ${famous_person}`;
// Print the message
console.log(message);
export {};
